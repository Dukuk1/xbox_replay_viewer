<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - XBOX ScreenNClips</title>

   
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
   
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
  
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    
    <style>
        /* Default to Dark Theme */
        :root {
            --xbox-green: #107C10;
            --bg-color: #121212;
            --card-bg: #1e1e1e;
            --card-border: rgba(255, 255, 255, 0.1);
            --text-color: #f8f9fa;
            --secondary-text-color: #adb5bd;
            --hover-bg: #2a2a2a;
        }

        /* Light Theme using prefers-color-scheme */
        @media (prefers-color-scheme: light) {
            :root {
                --bg-color: #f0f2f5;
                --card-bg: #ffffff;
                --card-border: rgba(0, 0, 0, 0.1);
                --text-color: #212529;
                --secondary-text-color: #6c757d;
                --hover-bg: #e9ecef;
            }
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
        }

        .profile-pic {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--xbox-green);
        }

        .gamertag {
            font-weight: 700;
            font-size: 2rem;
        }
        
        .hash-tag {
            font-size: 0.9rem;
            color: var(--secondary-text-color);
            letter-spacing: 1px;
        }

        .toolbar {
            border-top: 1px solid var(--card-border);
            border-bottom: 1px solid var(--card-border);
        }

        .btn-xbox {
            background-color: transparent;
            color: var(--text-color);
            border: 1px solid var(--card-border);
            transition: all 0.2s ease-in-out;
        }

        .btn-xbox:hover {
            background-color: var(--hover-bg);
            color: var(--text-color);
            border-color: var(--xbox-green);
        }
        
        .btn-xbox .bi {
            margin-right: 0.5rem;
        }

        .media-card {
            background-color: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 0.75rem;
            overflow: hidden;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }
        
        .media-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }

        .card-img-top {
            aspect-ratio: 16 / 9;
            object-fit: cover;
        }
        
        .card-body {
            color: var(--text-color);
        }
        
        .game-title {
            font-weight: 600;
        }
        
        .game-date {
            font-size: 0.85rem;
            color: var(--secondary-text-color);
        }

        .download-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background-color 0.2s;
        }
        
        .download-btn:hover {
            background-color: var(--xbox-green);
        }

    </style>
</head>
<body>
        


    <div class="container my-4 my-md-5">
        <!-- Profile Header -->
        <header class="d-flex align-items-center mb-4">
            <img src="<%= playSettings[1].value %>" alt="Profile Picture" class="profile-pic me-3">
            <div>
                <p class="mb-0 fs-4">Hello...</p>
                <h1 class="gamertag mb-0"> <%= user.gamertag %></h1>
                <span class="hash-tag">Hash: <%= auth.user_hash %></span>
            </div>
        </header>

        <!-- Toolbar -->
        <div class="toolbar py-3 mb-4 d-flex justify-content-between align-items-center">
            <button class="btn btn-xbox">
                <i class="bi bi-download"></i>
                Download All
            </button>
            <button class="btn btn-xbox">
                <i class="bi bi-filter"></i>
                Filter
            </button>
        </div>

        <!-- Media Grid -->
        <main>
            <div id="media-grid-container" class="row g-4">
            <% if (mediaItems.length > 0) { %>
                <% mediaItems.forEach(item => { %>
                <div class="col-12 col-md-6 col-lg-4">
                    <div class="media-card">
                         <div class="position-relative">
                           <a href="<%= item.url %>" target="_blank" rel="noopener noreferrer">
                                    <img src="<%= item.type === 'video' ? item.thumbnail : item.url %>" class="card-img-top" alt="Media from <%= item.title %>">
                                    <% if (item.type === 'video') { %>
                                        <i class="bi bi-play-circle-fill video-icon"></i>
                                    <% } %>
                                </a>
                                <a href="<%= item.url %>" target="_blank" rel="noopener noreferrer" class="btn download-btn">
                                    <i class="bi bi-download"></i>
                                </a>
                        </div>
                        <div class="card-body">
                            <p class="game-title mb-0"><%= item.title %></p>
                            <p class="game-date"><%= new Date(item.date).toLocaleDateString() %></p>
                        </div>
                    </div>
                </div>
                 <% }); %>
                  <% } else { %>
                <p class="text-center">No media captures found.</p>
            <% } %>
            </div>
        </main>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
